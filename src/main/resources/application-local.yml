spring:
  profiles:
    active: local
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    username: producter
    password: secret
    url: jdbc:postgresql://localhost:5432/producter

logging:
  level:
    ROOT: INFO
    pl.nowak.producter: DEBUG

